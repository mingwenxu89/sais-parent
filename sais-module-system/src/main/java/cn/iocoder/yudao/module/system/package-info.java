/**
 * Under the system module, we put general services to support the core business of the upper layer.
 * For example: users, departments, permissions, data dict, etc.
 *
 * 1. Controller URL: Start with /system/ to avoid conflicts with other Modules
 * 2. DataObject table name: starts with system_ to facilitate differentiation in the database
 */
package cn.iocoder.yudao.module.system;
