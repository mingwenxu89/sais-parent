<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>cn.iocoder.boot</groupId>
        <artifactId>sais-parent</artifactId>
        <version>${revision}</version>
    </parent>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>9</source>
                    <target>9</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- Required on Java 21: ByteBuddy 1.12.x only officially supports up to Java 20 -->
                    <argLine>-Dnet.bytebuddy.experimental=true -XX:+EnableDynamicAgentLoading -Xshare:off</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>sais-module-agri</artifactId>
    <packaging>jar</packaging>

    <name>${project.artifactId}</name>
    <description>
        Agri module, providing agricultural irrigation system features:
        crop management, plot management, IoT device management, sensor data,
        irrigation records, weather data, and alert information
    </description>

    <dependencies>
        <!-- System module (notify API, permission API, etc.) -->
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>sais-module-system</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- Business components -->
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>sais-spring-boot-starter-biz-tenant</artifactId>
        </dependency>

        <!-- Web related -->
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>sais-spring-boot-starter-security</artifactId>
        </dependency>

        <!-- DB related -->
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>sais-spring-boot-starter-mybatis</artifactId>
        </dependency>

        <!-- Utility related -->
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>sais-spring-boot-starter-excel</artifactId>
        </dependency>

        <!-- MQTT - HiveMQ (Netty-based, AWS IoT recommended) -->
        <dependency>
            <groupId>com.hivemq</groupId>
            <artifactId>hivemq-mqtt-client</artifactId>
        </dependency>

        <!-- AWS Bedrock Runtime - AI irrigation decision -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>bedrockruntime</artifactId>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-inline</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>
